        <% if book.favorited_by?(current_user) %>
          <td>
            <%= link_to book_favorites_path(book.id), method: :delete, class: "btn btn-danger" do %>
              ♥<%= book.favorites.count %>
            <% end %>
          </td>
          <% else %>
          <td>
            <%= link_to book_favorites_path(book.id), method: :post, class: "btn btn-primary" do %>
              ♡<%= book.favorites.count %>
            <% end %>
          </td>
        <% end %>